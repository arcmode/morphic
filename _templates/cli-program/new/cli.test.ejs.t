---
to: <%=locals.in%>/<%=name%>/__test__/cli.js
---
'use strict';

const cli = require('../lib/cli');

describe('<%= name %> cli', () => {
  // const argv = cli(cwd).parse(['args']);
  it('needs tests');
});
